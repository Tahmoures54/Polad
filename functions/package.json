{
  "name": "polad-functions",
  "private": true,
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit && node --check src/callables.js && node --check src/bankima.js",
    "test": "node --test test/finance.test.js",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "axios": "^1.8.4"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.13.10"
  }
}
